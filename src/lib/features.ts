/**
 * Feature flags for optional product surfaces.
 *
 * Set `AMICUS_VISIBLE` to `true` to re-enable the Amicus AI card, footer
 * link, audit modal, and all Amicus-related marketing copy across the site.
 */
export const AMICUS_VISIBLE = false;
